{
  "name": "neuralorem",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "neuralorem",
    "manifest": "src/manifest.json",
    "main": "neuralorem.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.2"
  },
  "author": "sven <sven.loechner@gmx.de>",
  "dependencies": {
    "@tensorflow/tfjs": "^0.13.4",
    "ml5": "^0.1.3",
    "sketch-module-web-view": "^2.1.1"
  }
}
